Very first time creating a repo and pushing the changes

1) git init 
2) git add . 
3) git commit -m 'message'
4) git remote origin <git-repo-name>
5) git push origin <name-of-branch>


** by default git creates master branch
git branch -M <new-branch-name>
git branch 

.(all files)

After you are modifying/updating the repo
git status

git add .
git commit -m <message>
git push origin <name-of-branch>


How to pull daily code changes from my repo

go to a certain path in gitbash (locate the location where youu want to clone my repo)
go to github (https://github.com/akshitagarg275/Soujanya_Python.git)

1) git fetch
2) git clone https://github.com/akshitagarg275/Soujanya_Python.git
3) git pull origin main
git pull origin <branch-name>



